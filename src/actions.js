import { createAction } from 'redux-actions';

export const fetchUser = createAction('FETCH_USER');
export const updateUser = createAction('UPDATE_USER');
export const putData = createAction('PUT_DATA');
